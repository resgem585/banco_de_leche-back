type User {
  id: ID!
  email: String!
  isAdmin: Boolean!
}

type Query {
  users: [User!]!
  login(email: String!, password: String!): User!
}

type Mutation {
  createUser(email: String!, password: String!, isAdmin: Boolean): User!
}
